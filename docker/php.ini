; Ajustes de PHP para producción (se suman a php.ini-production de la imagen).
expose_php = Off
memory_limit = 256M
opcache.enable = 1
; Los archivos no cambian dentro de un contenedor: no se comprueban en cada petición.
opcache.validate_timestamps = 0
opcache.max_accelerated_files = 20000
