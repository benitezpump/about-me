<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

/**
 * El contenido inicial vive en `db/seed/content.json` (o el de ejemplo) y se cargará con el importador del panel
 * (fase 3 de la migración, ver docs/decisions/0002). Por ahora no hay nada que sembrar desde aquí.
 */
class DatabaseSeeder extends Seeder
{
    public function run(): void {}
}
